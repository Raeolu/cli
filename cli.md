```
task create <name> <points> [{once/inf/daily/weekly/monthly}=once]

task <id> points <points>
task <id> done [day_offset=0]
task <id> delete
task <id> repeat <{once/inf/daily/weekly/monthly}> <start_date>

task filter todo
task filter done
task filter name <name>

event <id> delete
event <id> points <points>
```